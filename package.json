{
  "name": "mern-inventory",
  "version": "1.0.0",
  "description": "MERN Inventory Management App",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "start:pm2": "pm2 start ecosystem.config.cjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
